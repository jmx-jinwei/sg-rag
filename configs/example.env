# Copy this file to .env or export the variables in your shell.
# Do not commit real credentials or machine-specific paths.

PYTHON_BIN=python
DATASET_DIR=/path/to/UltraDomain
LLM_MODEL_PATH=/path/to/Meta-Llama-3-8B-Instruct
EMBEDDING_MODEL_PATH=/path/to/bge-m3
OUTPUT_DIR=outputs/sg_rag_run

# Optional runtime controls.
DOMAINS=mix
LIMIT=
TOP_K=10
EMBEDDING_BACKEND=hf
